# ===================================
# AI Infra Hub - Environment Variables
# ===================================
# 이 파일을 복사하여 .env를 생성하세요:
#   copy .env.example .env (Windows)
#   cp .env.example .env (Linux/Mac)
#
# .env 파일은 .gitignore에 포함되어 Git에 커밋되지 않습니다.
# 주의: .env 파일을 생성하지 않거나 키 값을 그대로 두면 보안이 취약해집니다.
# =================================================================

# Open WebUI 시크릿 키 (JWT 토큰 생성 및 민감 데이터 암호화에 사용)
# 반드시 32자 이상의 랜덤 문자열로 변경하세요.
# 미설정 시 Open WebUI 이미지의 하드코딩 기본값(t0p-s3cr3t)이 폴백으로 사용됩니다.
#
# 생성 예시 (Windows 환경):
#   python -c "import secrets; print(secrets.token_hex(32))"
#   openssl rand -hex 32
WEBUI_SECRET_KEY=your-secret-key-here-change-this
